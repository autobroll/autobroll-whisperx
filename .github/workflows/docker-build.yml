name: Docker - Build and Push

on:
  push:
    branches: [ main, master ]
    paths:
      - 'Dockerfile'
      - 'requirements.txt'
      - '**/*.py'
      - '.dockerignore'
      - '.github/workflows/docker-build.yml'
  workflow_dispatch:

# Empêche les builds concurrents sur la même branche
concurrency:
  group: docker-${{ github.ref }}
  cancel-in-progress: true

# Permissions minimales pour ce workflow
permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Prépare le nom d'image en minuscules : <dockerhub_user>/<repo>
      - name: Prepare image name
        run: |
          REPO_LC=$(echo "${{ github.event.repository.name }}" | tr '[:upper:]' '[:lower:]')
          echo "IMAGE_NAME=${{ secrets.DOCKERHUB_USERNAME }}/${REPO_LC}" >> $GITHUB_ENV

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}   # ex: autobroll
          password: ${{ secrets.DOCKERHUB_TOKEN }}      # ton PAT Docker Hub

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          platforms: linux/amd64
          tags: |
            ${{ env.IMAGE_NAME }}:latest
            ${{ env.IMAGE_NAME }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
