fastapi==0.111.0
uvicorn[standard]==0.30.1
aiohttp==3.9.5
numpy==1.26.4
ffmpeg-python==0.2.0

# PyTorch (CUDA 12.1) — doit matcher l'image GPU côté provider
torch==2.1.2
torchaudio==2.1.2

# WhisperX stable
whisperx==3.1.3

# Pydantic v2 (compatible avec FastAPI >=0.100)
pydantic==2.7.1
